@if (Visible)
{
    <div class="tw-alert-info @Class">@Text</div>
}

@code {
    [Parameter]
    public bool Visible { get; set; } = true;

    [Parameter]
    public string Text { get; set; } = "Загрузка...";

    [Parameter]
    public string? Class { get; set; }
}
